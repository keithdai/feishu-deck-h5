interface:
  display_name: "Publisher"
  short_description: "Publish confirmed feishu-deck-h5 HTML to Miaobi Magic Page."
  default_prompt: "Use $publisher to publish the confirmed HTML deck to Miaobi after user confirmation, using only publish artifact integrity checks. Do not ingest it into feishu-slide-library."

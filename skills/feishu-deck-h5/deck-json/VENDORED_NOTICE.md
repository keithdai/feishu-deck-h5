# Vendored third-party packages

`svg_to_pptx/` and `svg_finalize/` are vendored from
https://github.com/hugohe3/ppt-master (MIT License), so the pptx-exporter
subskill is self-contained — no ppt-master install needed. See each package's
`LICENSE` for the full MIT notice. Kept in sync upstream manually.
